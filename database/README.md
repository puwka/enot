# База данных

PostgreSQL без Supabase. Миграции в `database/migrations/`.

```bash
npm run db:migrate
npm run db:check
npm run db:seed
```

Администратор: `database/grant_superadmin.sql`

Подробная инструкция: `DATABASE_SETUP.md`
